const menuIcon=document.querySelector('.navbar-toggler');let clickCount=0;let lastClickTime=0;menuIcon.addEventListener('click',function(){clickCount++;const currentTime=new Date().getTime();if(clickCount===2 && currentTime-lastClickTime<60000){setTimeout(function(){const navbarCollapse=document.querySelector('.navbar-collapse');navbarCollapse.classList.remove('show');clickCount=0;},50);}lastClickTime=currentTime;});document.querySelector('.navbar-collapse').addEventListener('click',function(){clickCount=0;});document.addEventListener('click',function(event){if(event.target!==menuIcon){clickCount=0;}});